#ifndef MAIN_H
#define MAIN_H

#include <Arduino.h>

class main
{
public:
/*
    main();
    ~main();

    void setDebug(bool aDebug);
    bool main::isDebugging(void);

private:
    bool mainDebug = false;

    */
};


/*

void main::setDebug(bool aDebug)
{
  mainDebug = aDebug;
}

bool main::isDebugging(void)
{
  return mainDebug;
}


*/

#endif